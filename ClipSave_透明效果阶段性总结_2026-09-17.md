# ClipSave 透明效果阶段性总结

日期：2026-09-17

## 1. 当前阶段结论

ClipSave 的 Windows 透明效果问题已经从“视觉参数不好看”收敛为一个明确的窗口合成问题，并完成了 Win11 主路径的实机验证。

当前最重要的结论是：

1. Windows 11 22H2+ 的 `DWMWA_SYSTEMBACKDROP_TYPE = 3` 路径本身可以正常提供 Desktop Acrylic。
2. 之前 Win11 VM 中出现的大块深灰背景，不是 Acrylic 本身失败，而是 Qt 顶层窗口没有提供真正的 alpha backing surface。
3. 加入 `Qt.WA_TranslucentBackground` 后，DWM backdrop 能够穿过 Qt client surface，Win11 VM 中已经得到正常、实时、稳定的原生 Acrylic。
4. Win11 当前材质不是 Qt 自制模糊。模糊和背景采样由 Windows DWM 完成；Qt 只负责透明 client surface 和约 30% 的 UI tint。
5. 这次发现改变了对 Win10 旧测试结果的解释：过去 Win10 Acrylic 表现差，可能同时受 AccentPolicy Acrylic 本身和 Qt backing store 两个因素影响，因此 Win10 原生 Acrylic 值得在修正 alpha surface 后重新作为正式主路径使用。

## 2. 已完成的架构重构

### 2.1 Backdrop 结果结构化

原先单一 `bool` 返回值已经改为：

- `BackdropBackend`
- `BackdropResult`

窗口层现在可以知道当前真正生效的是：

- `SOLID`
- `LEGACY_BLUR`
- `DESKTOP_ACRYLIC`
- 预留的 `MICA`

同时保留 native error，便于失败诊断和 fallback。

### 2.2 Windows policy

已经覆盖：

- High Contrast
- Windows Transparency Effects
- Battery / Energy Saver
- `WM_SETTINGCHANGE`
- `WM_THEMECHANGED`
- `WM_DWMCOMPOSITIONCHANGED`
- `WM_POWERBROADCAST`
- `GUID_POWER_SAVING_STATUS`

系统状态变化会触发 backdrop 重新评估，而不是只在应用启动时决定一次。

### 2.3 Qt surface 与 native backdrop 解耦

Qt stylesheet 不再假设所有透明后端都需要同样的遮罩。

当前配方：

- Desktop Acrylic：低 tint，alpha 76，约 30%
- Legacy Blur：高 tint，alpha 204，约 80%
- Solid：顶层 surface 强制不透明

这样 DWM 已经提供完整材质时，Qt 不会再用 80% 白色/黑色遮罩把系统 Acrylic 盖掉。

## 3. Win11 实机测试发现

### 3.1 第一轮异常

Win11 VM 第一轮运行时，窗口没有显示正常 Acrylic，而出现接近 `#4C4C4C` 的大块灰色区域。

这与当前浅色 Acrylic tint 的 alpha 76 高度吻合：

```text
255 × 76 / 255 ≈ 76
```

也就是约 30% 白色被 Qt 合成到了黑色 backing store 上，而不是合成到 DWM Acrylic 上。

因此问题被定位为：

```text
DWM Acrylic 已在窗口后方
        ↓
Qt client surface 没有真正 alpha
        ↓
透明 stylesheet 先与黑色 backing store 合成
        ↓
DWM backdrop 被 Qt surface 挡住
```

### 3.2 修复

在 Windows 顶层 `QMainWindow` 创建 HWND 之前加入：

```python
self.setAttribute(Qt.WidgetAttribute.WA_TranslucentBackground, True)
```

新的链路变成：

```text
Qt / PySide6
    ↓
WA_TranslucentBackground
    ↓
透明 client surface
    ↓
Windows DWM Desktop Acrylic
    ↓
Qt 低 alpha tint
```

### 3.3 第二轮结果

Win11 VM 实测：

- Acrylic 正常显示
- 桌面内容能实时透过
- blur/tint 连续
- 无原先深灰 backing surface
- 拖动正常
- resize 正常
- 最大化/还原正常
- 最小化/恢复正常
- 当前约 30% tint 视觉效果良好

因此 Win11 Desktop Acrylic 主路径可以视为已实机验证。

## 4. 当前 Win11 技术路径

Windows 11 22H2+：

```text
Qt WA_TranslucentBackground
        ↓
DwmSetWindowAttribute
DWMWA_SYSTEMBACKDROP_TYPE = 3
        ↓
Windows DWM Desktop Acrylic
        ↓
Qt ~30% tint
```

这里 Qt 不负责 blur，也不负责背景截图。Acrylic material 本身来自 Windows compositor。

## 5. Win10 AccentPolicy Acrylic 已被性能测试淘汰

修正 Qt alpha surface 后，Win10 的旧 `ACCENT_ENABLE_ACRYLICBLURBEHIND = 4` 被重新做了完整 A/B 测试。结果证明过去的卡顿并不只是 Qt backing store 问题。

在真实 Windows 10 build 19044 / 144.001 Hz 上，最小 PySide 窗口测得：

| Backend | Move mean | Resize mean |
| --- | ---: | ---: |
| Solid | 6.942 ms | 7.007 ms |
| Legacy Blur (`AccentState=3`) | 6.943 ms | 7.098 ms |
| Accent Acrylic (`AccentState=4`) | 13.773 ms | 20.845 ms |

进一步控制实验表明：

- 静止 repaint 时 state 4 并不慢；
- 将窗口面积放大近 9 倍，state 4 的 move 时间仍几乎固定在 13.89 ms；
- native tint alpha 从 1 扫到 255，性能没有改善；
- wall time 大幅增加，但进程 CPU time 没有同比增长；
- 完整 ClipSave UI 下，state 4 resize 仍稳定表现出约 20.8 ms 的低 cadence。

因此旧 Win10 AccentPolicy Acrylic 的 move/resize compositor 路径本身就是主要瓶颈。它不再作为产品主路径。

## 6. Win10 modern Acrylic 已正式实现

在旧 state 4 被淘汰后，最终 Win10 路线改为系统自带的 `Windows.UI.Composition` HostBackdrop。中间曾尝试过 `DesktopAttachedSiteBridge + ContentIsland + DesktopAcrylicController`，几何 benchmark 很快，但最终桌面截图证明 ContentIsland 会盖在 Qt client content 上面，导致用户只能看到一整块 Acrylic、看不到任何 Qt 控件，因此该路线已经明确淘汰。

最终路径为：

```text
Qt WA_TranslucentBackground
        ↓
PySide HWND
        ↓
NativeAOT in-process bridge
        ↓
ACCENT_ENABLE_HOSTBACKDROP = 5
        ↓
Windows.UI.Composition Compositor
        ↓
non-topmost DesktopWindowTarget
        ↓
CreateHostBackdropBrush()
        ↓
tint SpriteVisual
        ↓
Qt client content 保持在 backdrop visual 上方
```

桥接层是 NativeAOT shared library，Python 通过 `ctypes` 在同一进程、Qt GUI 线程调用。`ICompositorDesktopInterop` 不再通过 NativeAOT 下不支持的自定义 `ComImport` 投影，而是直接走 COM `QueryInterface + vtable` ABI。最终用户不需要安装 .NET runtime；.NET 8 SDK 只属于构建工具链。

最终实现也已经完全移除 Microsoft Windows App SDK 依赖，不再需要私有 WinAppSDK runtime 或 reg-free WinRT manifest。

正式发布布局为：

```text
ClipSave/
├─ ClipSave.exe
└─ _internal/
   └─ windows_backdrop/
      └─ clipsave_windows_backdrop.dll
```

PyInstaller 最终 EXE 继续保留普通应用 manifest 中的：

- `asInvoker`
- Windows supportedOS 声明
- `longPathAware`
- Common Controls v6

构建脚本会自动：

1. `dotnet publish` NativeAOT bridge；
2. 只复制 `clipsave_windows_backdrop.dll`；
3. 生成 ClipSave 普通应用 manifest；
4. PyInstaller 嵌入 manifest；
5. 将 bridge 放进 `_internal/windows_backdrop`。

## 7. 统一后的 Windows 路径

当前正式设计为：

```text
Win11 22H2+
    → DWM System Desktop Acrylic
      DWMWA_SYSTEMBACKDROP_TYPE = 3

Win10 1809+
    → Windows.UI.Composition HostBackdrop
      ACCENT_ENABLE_HOSTBACKDROP = 5
      non-topmost DesktopWindowTarget

两者共同使用
    → WA_TranslucentBackground
    → ~30% Acrylic tint

Win10 modern Acrylic 不可用
    → ACCENT_ENABLE_BLURBEHIND = 3
    → Legacy Blur + ~80% tint

High Contrast / Transparency OFF
    → Solid
```

`ACCENT_ENABLE_ACRYLICBLURBEHIND = 4` 不再是正常 fallback，因为已经实测证明它在这台 Win10 19044 上存在明显 move/resize compositor cadence 问题。

## 8. 最终验证结果

### 8.1 自动化回归

正式集成后：

- 完整 unittest 回归：**530/530 tests passed**（最终工作树，195.801 s）
- 新增 visual-smoke analyzer 单测，专门防止“窗口存在但 Qt UI 被 backdrop 覆盖”的回归
- Windows `cmd.exe` / `build.bat` 捕获输出已改为 `locale.getencoding()` + `errors="replace"`，不再出现 `_readerthread UnicodeDecodeError`
- `py_compile`：通过
- NativeAOT publish：通过
- PyInstaller release build：通过
- portable ZIP：通过
- Inno Setup 未安装，因此本机未生成 installer

### 8.2 Win11

Win11 VM 已人工验证：

- DWM Desktop Acrylic 正常显示
- 实时背景采样和 blur 正常
- 无黑/灰 Qt backing surface
- move / resize 流畅
- 最大化/恢复、最小化/恢复正常

### 8.3 Win10 modern Acrylic 最小 packaged probe

Win10 19044 / 144.001 Hz：

```text
supported = 1
attach    = 1
move      ≈ 6.94 ms
resize    ≈ 7.06 ms
>16.7 ms  = 0 / 240
```

说明 Windows.UI.Composition HostBackdrop 能在同一台机器上避开旧 state 4 的约 72/48 Hz 行为，同时不会像被淘汰的 ContentIsland 原型那样遮住 Qt UI。

### 8.4 最终 ClipSave.exe

真正的：

```text
D:\GPT_WEB\clipsave\build\release\ClipSave\ClipSave.exe
```

smoke 结果：

```text
backdrop_backend=win10_composition_acrylic
backdrop_success=True
backdrop_native_error=None
quit_returned=True
event_loop_exited=0
```

所以最终发布产物在 Win10 19044 上确认没有悄悄 fallback 到 legacy blur。

最终桌面级 visual smoke 还直接抓取了真正显示在屏幕上的 1440×880 ClipSave 窗口：

```text
sampled colors = 530
edge pixels    = 914
visual_smoke   = PASS
backend        = win10_composition_acrylic
```

作为对照，被淘汰的 ContentIsland 空白窗口版本只有 105 个 sampled colors / 41 个 edge pixels。因此现在不是只验证 Qt 的 `isVisible()`，而是确认最终桌面合成画面里真的存在按钮、侧栏、文字和内容结构。

对最终 packaged ClipSave HWND 又进行了两轮独立的 240 次 move / 240 次 resize 几何测试：

```text
Round A
  move   mean 6.943 ms, p95 7.172 ms, p99 9.160 ms
         >16.7 ms = 0/240, >33.3 ms = 0/240
  resize mean 7.760 ms, p95 13.845 ms, p99 22.003 ms
         >16.7 ms = 6/240, >33.3 ms = 0/240

Round B
  move   mean 6.971 ms, p95 7.179 ms, p99 9.828 ms
         >16.7 ms = 0/240, >33.3 ms = 0/240
  resize mean 6.981 ms, p95 8.534 ms, p99 11.036 ms
         >16.7 ms = 0/240, >33.3 ms = 0/240
```

两轮都没有任何 >33.3 ms 样本。Round A 的 resize 有少量孤立 >16.7 ms 抖动，但 Round B 完全没有，说明它不是旧 state 4 那种稳定的低 cadence。正式 EXE 的主体分布已经回到约 144 Hz 单刷新周期附近；旧 Accent Acrylic 固定在约 13.8 / 20.8 ms 的性能特征已经消失。

最终包内容也重新审计：

- `_internal/windows_backdrop/` 只有 `clipsave_windows_backdrop.dll` 一个文件；
- release 目录和 portable ZIP 都不再包含 Windows App SDK / WindowsAppRuntime 文件；
- NativeAOT bridge 不再引用 `Microsoft.WindowsAppRuntime.dll`；
- 最终 EXE 的 manifest 仍保留 `asInvoker`、Windows supportedOS、Common Controls v6、`longPathAware`；
- manifest 中已经没有 WinAppSDK 私有 runtime 注册。

## 9. 当前封板结论

这一阶段的透明系统可以封板：

- Win11：原生 DWM Desktop Acrylic
- Win10：Windows.UI.Composition HostBackdrop Acrylic
- 两者都使用真正 alpha-capable Qt top-level surface
- Legacy Blur 仅为兼容 fallback
- Solid 负责系统策略/无透明场景
- 不切换到 Mica
- 不需要把 Qt 自制截图模糊作为默认 Win10 路径

当前约 30% tint 已在 Win11 实机表现良好，Win10 modern Acrylic 也沿用同一套 Acrylic UI recipe。后续若再调整透明系统，应优先视为视觉微调或兼容性维护，而不是重新更换主架构。
